<div align="center">

**English** · [한국어](CHANGELOG.ko.md)

# Update notes

Everything between the spec being frozen in [DESIGN.md](DESIGN.md) (2026-07-20) and **v1.11.0** (2026-08-06) — all **236 commits**.
The hash at the end of each entry points at the original commit.

</div>

---

## Contents

- [Timeline](#timeline)
- [v1.11.0](#v1110--2026-08-06-cdc60ff) · [v1.10.0](#v1100--2026-08-05-b245db9) · [v1.9.1](#v191--2026-08-05-1984531) · [v1.9.0](#v190--2026-08-05-6b9b4ca)
- [v1.8.0](#v180--2026-08-04-1c9a70c) · [v1.7.0](#v170--2026-08-04-94f9791) · [v1.6.0](#v160--2026-08-04-d7af8ef) · [v1.5.0](#v150--2026-08-04-17a1e3a) · [v1.4.0](#v140--2026-08-03-5e7a59b)
- [v1.3.1](#v131--2026-08-03-0f736c0) · [v1.3.0](#v130--2026-07-31-68e920c) · [v1.2.0](#v120--2026-07-31-de73205) · [v1.1.1](#v111--2026-07-31-6d2d8c5) · [v1.1.0](#v110--2026-07-31-8d01636)
- [Early development (2026-07-20 → 07-31)](#early-development--2026-07-20--07-31)
- [Where it diverged from the original design](#where-it-diverged-from-the-original-design)

---

## Timeline

| Version | Date | Commits | Headline |
|---|---|---|---|
| [v1.11.0](#v1110--2026-08-06-cdc60ff) | 2026-08-06 | 3 | Live thinking/token meter, growing composer with markdown, clipboard fallback |
| [v1.10.0](#v1100--2026-08-05-b245db9) | 2026-08-05 | 3 | Tasks panel — subagents, background shells, workflows |
| [v1.9.1](#v191--2026-08-05-1984531) | 2026-08-05 | 3 | Bypass mode dying under root, resume id lost mid-stream |
| [v1.9.0](#v190--2026-08-05-6b9b4ca) | 2026-08-05 | 12 | Admin self-update, Git diffs + history graph, pull, Docker probe, My Page tabs |
| [v1.8.0](#v180--2026-08-04-1c9a70c) | 2026-08-04 | 3 | Floating guide agent that explains *and* does the thing |
| [v1.7.0](#v170--2026-08-04-94f9791) | 2026-08-04 | 11 | Signature waiting animation, chats grouped by project, self-assembling context menu, webhook PR review, custom branding, skill counters |
| [v1.6.0](#v160--2026-08-04-d7af8ef) | 2026-08-04 | 12 | Import collision handling, git init & publish, remote management, manual naming |
| [v1.5.0](#v150--2026-08-04-17a1e3a) | 2026-08-04 | 4 | Auto-fetched model list, 5-hour auto-resume, window primer |
| [v1.4.0](#v140--2026-08-03-5e7a59b) | 2026-08-03 | 17 | Unified search (Ctrl/Cmd+K), shortcuts, own context menu, auto session names, collapsible sidebar |
| [v1.3.1](#v131--2026-08-03-0f736c0) | 2026-08-03 | 2 | Privacy master switch overrides and locks the per-channel toggles |
| [v1.3.0](#v130--2026-07-31-68e920c) | 2026-07-31 | 2 | Per-channel toggles (9) for non-essential egress |
| [v1.2.0](#v120--2026-07-31-de73205) | 2026-07-31 | 12 | Non-essential Anthropic egress blocked by default, big README overhaul |
| [v1.1.1](#v111--2026-07-31-6d2d8c5) | 2026-07-31 | 3 | Multi-arch (amd64/arm64) buildx, Docker Hub overview page |
| [v1.1.0](#v110--2026-07-31-8d01636) | 2026-07-31 | 4 | Release pipeline + Docker Hub publishing, standalone compose, self-provisioned network |
| [Early development](#early-development--2026-07-20--07-31) | 07-20 → 07-31 | 144 | P0–P5 skeleton · LLM Wiki · per-user tokens · git · PR review automation · admin config · session import · DM |

---

## v1.11.0 — 2026-08-06 (`cdc60ff`)

- **Live thinking/token meter, growing composer with markdown, working copy (`d0aaf40`)** — four strands in one commit:
  - fix(copy): **every copy button was dead outside a secure context.** `navigator.clipboard` does not exist over plain `http://` on a LAN address, which is how this workspace is normally reached. New `lib/clipboard.ts` copies via the API when present and falls back to a selection + `execCommand('copy')`; all call sites (answer copy, rendered code blocks, right-click menu, webhook fields) route through it, and a failed copy now says so instead of doing nothing
  - fix(chat): editing a message no longer requires changing it — re-sending the same text truncates from that point and regenerates, same as any other edit
  - feat(chat): a turn now reports what it is doing — "Thinking…" while extended-thinking deltas stream, plus an output-token meter that climbs live (char estimate) and snaps to the SDK's exact per-message count on `turn:usage`
  - feat(chat): the composer and the message-edit box grow with their content up to a ceiling, then scroll. **Live markdown in both** — a highlight mirror painted behind the transparent textarea, so the caret, IME composition and the `/` and `@` menus are untouched. Only width-preserving styles are used (colour, background, text-stroke faux bold), or the caret would drift off the glyphs; `md.test.ts` pins the invariant that highlighting is purely additive
- **Per-turn slot/ttft/total timing logged (`acc2336`)** — turn latency was unattributable: no way to tell a turn blocked on the global `maxConcurrentTurns` cap from one where the CLI/model itself was slow. One line per turn: slot (semaphore wait), ttft (spawn + model time to first visible output), total, tokens, cap usage. Also guards `endRunningTasks` in the `finally` — a throw there would replace the turn's own outcome and skip the sandbox teardown below it

## v1.10.0 — 2026-08-05 (`b245db9`)

- **Tasks panel (`f1051b3`)** — a turn's Task-tool subagents, backgrounded shells, local workflows and MCP monitors never appeared anywhere in the UI: the CLI reports them as `system` messages (`task_started` / `task_progress` / `task_updated` / `task_notification` / `background_tasks_changed`), which the turn stream dropped on the floor. Their nested tool calls DID show up — indistinguishable from main-thread ones.
  - `server/src/claude/tasks.ts` folds those events into one ordered list per chat session and broadcasts the whole list on each change (`tasks:update`, replace semantics so a missed edge can't wedge a stale running row). `session:join` replays it, and `runTurn`'s `finally` settles anything still running — the CLI subprocess dies with the turn, so nothing it spawned can outlive it
  - Web: a "Tasks" header pill (live count, glints while work runs) opens a resizable right-side panel with per-kind filter tabs, status, elapsed time, token/tool-call counts, the tool each task is on, and its summary or error. Full-screen overlay on a phone. Subagent tool calls now carry `parentId` and get badged in the transcript
  - Admin: `taskPanelEnabled` / `taskHistoryMax` / `taskSessionsMax`
- merge: `feat/task-panel` (`bf5598d`)

## v1.9.1 — 2026-08-05 (`1984531`)

- **fix(claude): bypass mode no longer kills the turn under root (`67b4ff8`)** — `bypassPermissions` maps to the CLI's `--dangerously-skip-permissions`, which the CLI refuses when the process is root ("cannot be used with root/sudo privileges"). The app container runs as uid 0, so every turn in bypass mode — and every `probeCommands`/`probeUsage` on such a session — died with "process exited with code 1". `buildOptions` now downgrades the SDK mode to `acceptEdits` when running as root, and `makeCanUseTool` auto-allows every tool in bypass mode, so the mode keeps its never-prompt behaviour (the class-1 path fence still applies). Same approach the wiki compile path already used
- **fix(claude): keep the resume id when a turn dies mid-stream (`2b8b24b`)** — `claude_session_id` was written only on the success path, so any turn that errored, was interrupted, or died with the container left the column null. The CLI transcript was already on disk under the session's HOME, but with no id to resume the next message started a brand-new conversation — Claude with zero context while the UI still rendered the full history from our DB, **which makes the loss invisible**. `runReal` now reports `session_id` as soon as the CLI emits it and `runTurn` persists it immediately; the capture also moved ahead of the abort check so a turn stopped on its first message keeps its transcript

## v1.9.0 — 2026-08-05 (`6b9b4ca`)

- **Admin self-update (`e2506cd`)** — an Update tab compares the running version against the newest semver tag published for the app's own image (Docker Hub), then updates the workspace from inside the workspace. A container cannot recreate itself, so the swap runs in a **throwaway helper container** started from the freshly pulled image:
  1. create the replacement under a temp name — validates the whole create spec while the old container is still serving, so a bad spec means no downtime
  2. graceful stop + remove the old one, letting SQLite checkpoint (no SIGKILL)
  3. rename temp → real name and start it
  4. watch it for `selfUpdateHealthWaitMs`; if it exits or crash-loops, remove it and restore the previous image
  - The create spec is rebuilt from our own inspect output so ports, mounts, env, labels, networks and the restart policy carry over. Two fields are deliberately not copied verbatim: an auto-assigned Hostname (the old container's short id, which would break the new instance's self-lookup) and Cmd/Entrypoint when they merely mirror the old image's defaults (copying those would pin the new image to the old startup command). Rollback targets a dedicated `:ccw-previous` tag rather than a bare image id
  - The outcome is reconciled at the next boot by comparing our own image id against the ids recorded before the swap, so nothing has to survive in memory; failures keep the helper's log for the panel to show. The panel polls through the downtime and reloads itself once the new version answers
  - `GET /api/admin/update`, `POST /api/admin/update/check|apply` (admin-only, gated by `selfUpdateEnabled`, pull restricted to the app's own repo). Config group update: `selfUpdateEnabled`, `selfUpdateAutoCheckMs`, `selfUpdateCheckTimeoutMs`, `selfUpdateHealthWaitMs`, `selfUpdateContainer`. The periodic check only refreshes a cache — it never applies anything by itself. Verified against real Docker (swap + rollback)
- merge: `feat/self-update` (`80db2c6`)
- **fix(deps): add vitest so typecheck stops failing (`68b2906`)** — `images.test.ts` (and `self-update.test.ts`) import from `vitest`, which was never a dependency, so `npm run typecheck` failed with "Cannot find module 'vitest'" regardless of the change under test. Root devDependency on purpose: the runtime image installs `-w server` without `--include-workspace-root`, so vitest never ships. No `test` script — the other `*.test.ts` files are standalone `npx tsx` scripts, so a repo-wide `vitest run` fails on them
- **feat(docker): probe the daemon and surface it instead of failing on use (`29422de`)** — three features hang off the Docker daemon (code-server editors, PR review sandboxes, self-update) and each only failed at the moment of use with a raw dockerode error. The existing check only asked whether `DATA_VOLUME`/`CODE_SERVER_NETWORK` were set, so a deploy whose socket is missing or whose daemon is down passed it and broke later.
  - `lib/docker-status.ts` pings the daemon at boot and every `dockerProbeMs`, caches the verdict, and classifies the failure into something an operator can act on: socket-missing / denied / unreachable / unconfigured
  - Boot log warns with the reason and names the disabled features; `GET /api/admin/overview` carries `docker` (+ `POST /api/admin/docker/probe` to re-probe); admin Overview shows a banner (reason, what stops working, how to fix it, the raw error, a Re-check button); `GET /api/config` carries `dockerReady`/`dockerReason`, so the chat header disables the split/editor views with the reason as their tooltip and a remembered 'editor' view is coerced back to chat; the editor endpoint's 501 names the reason
  - Chat, projects, wiki, search and DMs are unaffected, which the banner says explicitly. Reason precedence lives on the server only: a ping failure outranks "env unset"
- **feat(web): split My Page into tabs like the admin panel (`c240899`)** — nine stacked sections were one long scroll → profile / session / requests / credentials / projects. The session tab hides when all three automation toggles are disabled, requests when approvals are off
- **fix(web): stop the composer reserving dead space, centre the guide input (`cd0df76`)** — every composer padded a blanket `pr-14` so the floating guide launcher could not cover Send, even on a wide screen where the centred 760px card already clears it. `useGuideInset` measures the row instead (0 when it clears the launcher), re-measured on every commit plus a ResizeObserver and window resize. The guide panel's textarea also parked its text on the row's bottom edge — `py-1.5` lines the 20px box up with the 32px send button
- **feat(git): pull a project from origin, and open the Git panel from My Page (`dcf5013`)** — the panel could commit and push but never fetch, so a project that moved on the remote had no way back short of the terminal. `POST /api/projects/:id/git/pull` — fast-forward only by default (so it never invents a merge commit in someone's workspace), `{ rebase: true }` replays diverged local commits on top with `--autostash`. Plus a Pull button and rebase toggle; My Page's project list gets a per-project Git button so a project need not be attached to a chat first
- **feat(git): pull with `--all` (`56ec8ac`)** — a plain pull only updates the current branch's upstream, so a branch created on the remote stayed invisible. Pull now fetches every remote in the same pass and widens a `--single-branch` clone's refspec first, or `--all` still could not see the other branches. git refuses `--all` next to a refspec ("fetch --all does not make sense with refspecs"), so the no-upstream case fetches all remotes in a separate step and keeps its explicit `origin <branch>`. The panel shows the tail of git's output instead of only its last line — with `--all` the `* [new branch] …` lines are the interesting part
- **feat(git): per-file diffs and a lane-drawn commit history graph (`ab118bc`)** — the panel could stage and push a change but never show it. The file name in the change list is now a button that opens the patch, and a History section draws the commit graph (branches, merges, refs) from `git log --topo-order` with a colour lane per line of development — clicking a commit shows its stat + patch. Two read-only endpoints back it (`GET /git/log`, `GET /git/diff`), both validating rather than escaping (execFile never involves a shell): a commit must look like a sha so it cannot be a flag or an arbitrary ref, and a path cannot be absolute or contain `..` — the untracked case reads the file off disk, since git has nothing to diff it against. Lane layout lives in `web/src/lib/gitgraph.ts` so it runs under plain tsx: a commit reached from two lanes collapses them, or the graph would creep rightwards forever. `gitLogMaxCount` / `gitDiffMaxKB` are admin settings, not constants
- **feat(web): a full-screen toggle for the Git dialog (`dea68b3`)** — a 560px dialog was fine for staging and pushing, but the history graph and a patch are exactly the content it crops. One title-bar button trades the fixed width for 96vw × 94vh, and the boxes inside grow with it (patch 18rem → 58vh, graph 16rem → 40vh, change list 13rem → 30vh). `fullscreen`/`titleExtra` are optional Modal props, so the other nine dialogs render exactly as before
- merge: `feat/git-diff-graph` (`534d2db`)

## v1.8.0 — 2026-08-04 (`1c9a70c`)

- **Floating product-guide + control assistant (`ff4cd1f`)** — a round button in the bottom-right corner opens a small chat panel that both explains the product and carries requests out. The agent's whole tool surface is **two** in-process MCP tools:
  - `api` re-enters this Fastify app through `app.inject()` with the caller's own session cookie, so each route runs its normal `requireAuth`/`requireAdmin`/ownership checks — permission enforcement is identical to clicking the UI, there is no second copy of the rules. An allowlist (`server/src/guide/api-map`) narrows it further: **no DELETE at all**, no credential/secret routes, no admin infrastructure verbs. The same table renders the agent's API reference, with admin routes filtered out for members
  - `ui` covers what has no API (language, theme, navigation, dialogs) by pushing a `guide:action` to every tab of that user
  - Every built-in tool (Bash/Read/Write/…) is denied twice: `disallowedTools` plus a `canUseTool` that only ever allows the two tools above
  - Thread state lives in its own per-user tables (`guide_threads`/`guide_messages`) rather than `chat_sessions`, whose viewer check falls through to true for unknown kinds. Streaming rides the existing `user:<id>` socket room
  - Admin settings: `guideEnabled`, `guideWriteEnabled` (read-only mode), `guideModel`, `guideMaxTurns`, `guideHistoryMax`, `guideMaxInputChars`, `guideMaxToolChars`
  - Plus ko/en strings, static-demo parity (canned turns for the suggestion chips, including a real language switch and session creation), and `pr-14` on the chat + DM composers so the launcher never covers Send
- merge: `feat/guide-agent` (`8e170e2`)

## v1.7.0 — 2026-08-04 (`94f9791`)

- **feat(ui): signature waiting animation for every model wait, naming included (`f8b645b`)** — replaces the generic blinking dots with one mark used everywhere a model call is in flight: the brand mark's three dots (favicon.svg) travelling as a wave down its own clay tint ladder, a clay glint running through the label next to them, and a turning clay ring around the sparkle for naming calls.
  - `ClayDots`/`ClaySpark`/`ClayWait` in `lib/ui.tsx` + keyframes in `styles/index.css` (`--clay-mid`/`--clay-pale` tokens, reduced-motion freezes to a static badge)
  - Applied to the streaming answer, the wiki compile line, the composer hint, the queued-turn banner, and the retitle button in the header + sidebar rows
  - **Session titling was invisible until now**: auto-title/retitle/import emit `session:titling {on}` around the call, so the row title, the header title and every naming button wait together (`store.titling`, one source of truth, cleared in `finally` so the mark can never stick)
  - Demo mirrors the titling event pair, and `route()` may answer late so a canned model call still shows its wait
- **feat(ui): group sidebar chats by project with collapsible headers (`5567ec4`)** — private chats filed under their project (common first, then personal, unassigned last) instead of one flat list. Each project header folds its chats away; the fold state persists in localStorage. `setProject` also patches the session list so a row moves group instantly
- **feat(plugins): per-user skill usage counters in the skill detail (`4861dae`)** — count which skills each turn invoked, per user, and surface the numbers where skills already live: expanding a skill in a plugin's detail modal shows the workspace total, the viewer's own count, and (admins only) the per-user breakdown.
  - `skill_usage` table: one counter row per (user, skill key), upserted per turn. Keys are recorded raw and matched to a plugin's skills at read time (`skillKey` collapses `plugin:skill`, `plugin/skill` and bare `skill`)
  - Both invocation paths counted: a prompt that is a slash command (what the composer palette sends) and Skill / SlashCommand tool calls mid-turn
  - `skillUsageEnabled` (default on) gates counting AND the UI; orphan rows of deleted users join the resource-cleanup sweep
- **feat(ui): the right-click menu builds itself from the clicked element (`366a986`)** — the context menu was wired per surface, so only four places had one. Now it is assembled from whatever was right-clicked:
  - `mirrorRows()` — the clicked row's/card's own controls, read off the DOM by aria-label/title/text, with their icon cloned and danger styling inferred. Selecting a row clicks the real button, so the surface keeps owning the handler. Sibling rows are excluded, so a project header can't offer to delete the chats under it
  - `dataRows()` — clipboard rows for a selection, a field, a link, an image, a code block, or a tree row's full path
  - `appRows()` — the app-wide rows, as before
  - Groups merge with dedup by label (a surface's own row wins) and separator collapsing. That made most hand-written menus dead code: chat messages lose theirs entirely, sidebar rows keep only "Open", and the admin-only delete on wiki topics is now gated by **the button's own presence** instead of a duplicated `isAdmin` check. The menu scrolls when long (Shift+right-click hint stays pinned) and scrolling inside it no longer closes it. `GroupHeader` gets the aria-label it was missing; `turnSkillKeys`' block type widened because `npm run typecheck` was red on main
- **feat(wiki): add + edit raw sources on an existing topic (`0cfb7bf`)** — admins can keep a compiled topic current without recreating it: the file explorer's raw/ tab gets a drop zone (files or whole folders) and an inline editor for existing text sources.
  - Server: `PUT /api/wiki/topics/:id/file` writes one raw/ text file in place (path sanitized, text-only, capped by `wikiEditMaxKB`); the existing add-sources POST **no longer auto-recompiles** — uploads are one request per file, so N compiles would race and the inflight guard would drop the ones carrying the later files. The client recompiles once instead
  - Config: `wikiSourceEditEnabled` (endpoints 403 + UI hidden), `wikiEditMaxKB`
  - Web: `FileExplorer` grows optional `uploadDir`/`onUpload` + `editDir`/`onSave`, so the tree/preview pane is reused instead of a second modal; `WikiExplorer` wires them for admins and shows a "recompile needed" bar. Drag-drop collection moved to `lib/dropfiles` (was duplicated in Sidebar and ImportSessionModal, would have been a third copy)
- **feat(wiki): open the source manager from the sidebar topic row (`675711f`)** — the explorer was only reachable from a topic's chat banner, so managing sources meant switching threads first. Admins now get a folder button on every sidebar topic row; the right-click menu mirrors it automatically (`ctxrows` reads the row's buttons), so touch users reach it by long-press
- **feat(brand): admin-set custom logo + workspace title (`ba7ebb6`)** — the name and mark were hardcoded in three places. An admin uploads a logo and sets the title once (Admin → Config → Branding); both apply live for everyone across sidebar, login card, landing screen and browser tab.
  - `brandTitle` / `brandLogoMaxMB` config keys; the logo is stored as `<dataDir>/brand/logo.<ext>` with its mtime as the cache-bust token (no DB column)
  - `GET /api/brand` and `GET /api/brand/logo` are public so the login card is branded before sign-in; logo responses carry nosniff + a locked-down CSP so **an uploaded SVG cannot script this origin**
  - Image mime/magic validation extracted to `lib/images.ts` and shared with the avatar upload (which stays raster-only; SVG is opt-in per call site)
- **feat(review): webhook-triggered PR review, per-repo secret (`2130a27`)** — providers can push PR events instead of waiting for the poll interval: `POST /api/review/hooks/<repoId>`, authenticated per repo by GitHub HMAC (`X-Hub-Signature-256`), the GitLab secret-token header, or `?token=` for Bitbucket (no secret field of its own). Only PR events poll; comment/push noise is answered 200-and-ignored. Admins issue/rotate/clear the secret from the repo edit dialog (URL + secret with copy + per-provider setup hint). Also coalesces polls: a request arriving mid-poll re-runs once afterwards instead of being dropped — that hole is fatal for webhook-only deploys with `REVIEW_POLL_MS=0`
- **feat(review): per-repo polling toggle (`226aef7`)** — `REVIEW_POLL_MS=0` was all-or-nothing. `review_repos.poll_enabled` lets the interval poller skip individual repos, so a webhook-wired repo stops polling while repos you couldn't wire keep it. Webhook deliveries and the manual "refresh now" still call `pollRepo` directly — only the interval tick honours the flag. Sidebar marks a skipped repo "webhook only"
- **feat(review): decide webhook + polling when registering a repo (`716a1f7`)** — the webhook was edit-dialog-only, so every new repo started webhook-less. The add dialog now carries both switches (polling on, webhook off by default), `createRepo` issues the secret when asked, and the URL/secret are shown right after creation — that is when the admin needs them. Asking for a webhook while `reviewWebhook` is off is refused (403), not silently downgraded to a repo whose hook never fires

## v1.6.0 — 2026-08-04 (`d7af8ef`)

- **feat: name imported local sessions after their own conversation (`cb9cb5e`)** — transcripts the CLI never named landed as a raw uuid in both the import picker and the resulting chat row. Two layers: `listSessions` falls back to a cleaned snippet of the first user message (and reports `custom` so a CLI-set title is never overwritten); once the import response is out, each snippet-named chat gets the same model titling pass a fresh chat gets, reading the first few user turns, with the new title arriving over `session:title`. `auto-title.ts` grows `autoTitleImported` on a shared `titleFor` core, and its title sanitizer moves to `lib/session-import.ts` so both callers use it. New knobs: `importAutoTitleEnabled`, `importAutoTitleMessages`
- **feat(import): make imported-session titling a choice on the import screen (`b93c07d`)** — reading each transcript to name it costs one model call per session, so it should not just happen. The screen carries a checkbox (seeded from the user's own auto-naming preference); the server runs the pass only when the request asks for it, and the admin flag still gates whether the choice is offered at all. Without it, imported sessions keep the first-message snippet — still never a raw uuid
- **fix(import): drop CLI plumbing lines from imported transcripts (`d105d33`)** — an imported session ended with a raw `<local-command-caveat>` block: the CLI files its own injected lines as `type:"user"`, and `jsonlToMessages` only skipped `isSidechain` and the meta *types*, so `isMeta` lines came through as real chat messages. Slash-command wrappers and captured local-command stdout landed the same way. Both are dropped at the one place every caller routes through, so the message rows and the generated title are clean. A line is only discarded when nothing but the tags is left — a genuine message that merely quotes one survives
- **fix(import): make an imported `/clear` or `/compact` fold the history above it (`0c06e3f`)** — Chat.tsx folds a segment when a user message starts with `/clear` or `/compact` — the plain form our own composer sends. The CLI files the same action as `<command-name>/clear</command-name><command-args>…</command-args>`, which never matched, so an imported session showed the tag soup (and, after the plumbing filter, nothing at all) with no fold. Slash-command lines are rewritten to `/name args` on import; `userTexts` skips them, so a command can never become the generated title
- **feat(import): flag already-imported sessions, pick overwrite or clone per session (`b74f044`)** — nothing keyed `chat_sessions` by `claude_session_id`, so re-importing a folder silently produced a second copy of every session it already held. The staging list now reports `dup` for transcripts this user owns; the picker badges them and puts an Overwrite / Add a copy select on each row. Overwrite reuses the existing chat id — its messages are replaced and it re-points at the freshly uploaded project — so links and history survive. The select sits outside the row label, since inside it every interaction would toggle the checkbox. The demo router serves a seeded list with two duplicates
- **feat(import): choose overwrite or clone for the project too (`1376bd7`)** — the project half always cloned: a name collision quietly became `myproj-2` and a second projects row, so re-importing left two copies of the same working dir. The picker flags a name the user already owns and offers the same choice. Overwrite reuses that row and path — the upload is copied OVER it, so files at the same path are replaced and everything else survives. Deliberately not a wipe: `.git`, untracked work and editor state live in that directory. Sessions imported alongside then resume against the existing path, since the slug is derived from it
- **fix(import): settle the project on the step that uploads it (`fc6b082`)** — the project name and its overwrite/clone choice sat on the final step, after the `~/.claude` folder pick — but the project is already uploaded when the tree step ends. Skipping the session folder was worse: the flow then asked for a name by hand at the end, for a project that had been on disk since the previous step. The name is now asked on the tree step, prefilled from the folder the user actually picked (`stripRoot` already knew that name and threw it away). The final step just recaps "Project: x · Overwrite"
- **feat(git): git init and publish a project that is not a repository yet (`9519395`)** — an imported project lands as plain files, so its Git panel was a dead end. The panel now offers both ways out: init alone, or publish (init → first commit → create the repository on the provider through the credential the user already registered → push). A pasted URL skips creation, and is the only route for a provider whose API we do not speak. Repo creation lives in `lib/git-publish.ts` (GitHub incl. Enterprise, GitLab, Bitbucket); `git-ops` gains `gitInit`/`gitHasCommits`/`gitSetOrigin`, all no-ops on the parts already done, so publishing an already-tracked project never rewrites its history. The remote is wired up only after creation succeeds. Credential ids from the body are re-checked against the caller. Knobs: `gitPublishEnabled` (gates the endpoints, not just the buttons), `gitInitBranch`
- **feat(git): manual remote management per project (`7dd4e90`)** — the panel could only ever talk to whatever origin a clone happened to have. It now lists the project's remotes and lets you add, retarget and remove them — collapsed by default. Every mutation returns the fresh list and reloads the panel's status, because retargeting origin also changes which credential push resolves to. `git remote add` takes no `--` separator, so names and URLs are **validated rather than escaped**: a leading `-` would be read as a flag. URLs are restricted to http(s)/ssh/git and scp-like `user@host:path` — a bare local path or `file://` is rejected on purpose, since as a remote it would let one user fetch out of another user's project directory, and git's `ext::` transport runs a command. `lib/git-ops.test.ts` covers those cases; the publish route validates its pasted URL through the same function. Also fixes the branch row overflowing at 375px
- **feat(import): choose whether overwriting a project keeps or deletes its files (`335fa6b`)** — overwriting always merged, so a re-import of a moved project leaves stale files behind forever. The picker now asks, next to the overwrite choice: keep (merge, unchanged) or delete (empty the folder first). Delete is never the default, only runs when there are files to put back, and its hint switches to a warn-coloured warning naming what goes: the `.git` history and any uncommitted work. The directory itself is kept rather than recreated, since a code-server container may have it mounted and the path is recorded in the projects row. `emptyProjectDir` refuses any path not strictly under the caller's own projects root — the path comes from our own row, but the operation cannot be undone, so it is guarded rather than trusted
- **feat: manual "name this chat" button (`67ee010`)** — automatic naming fired once, on the first turn of a chat still carrying the placeholder, so any chat past that point was stuck — including every chat imported before the naming pass existed. There is now a ✨ button in the chat header and on each sidebar row, plus a context-menu row next to Rename. `retitleSession` deliberately drops the guards `maybeAutoTitle` needs: **pressing the button IS the request**, so neither the placeholder title nor the user's preference gates it, and it overwrites what is there. It reads several turns instead of just the first, since a conversation that already ran is not described by its opening message, and it throws a reason the UI shows (no auth, nothing said yet, not a private chat) rather than failing quietly the way the automatic path must. The header button is the one that works on a phone

## v1.5.0 — 2026-08-04 (`17a1e3a`)

- **feat: auto-fetch the model list from the provider's `/v1/models` (`c5c5f5d`)** — frontier model ids change often, so the hardcoded `models` map goes stale. The server pulls the live list (newest first, capped by `modelsMax`) from the configured provider — api.anthropic.com or a custom base URL — and writes it back into the same `models` config every consumer already reads. Runs at boot + on `modelsRefreshMs`, or on demand from the admin panel's [Fetch now]. `defaultModel`'s select options track that map instead of a frozen array
- **feat: auto-resume a turn when the claude.ai 5-hour window resets (`7bc5495`)** — a turn that dies on the plan limit (5-hour / weekly) is not a transient 429, so `withRateLimitRetry` cannot help and the prompt was simply lost. It is now parked in a new `pending_resumes` table, the composer shows the scheduled retry time (cancellable), and the server re-enqueues it once the window reopens. Timers are re-armed at boot, so a restart inside the wait does not drop the prompt; rows overdue by more than `autoResumeStaleMs` are discarded instead of replayed. **Claude-subscription only by construction**: eligibility requires `CLAUDE_CODE_OAUTH_TOKEN` in the resolved provider env, so API keys and bedrock/vertex/custom providers are never parked. Review sessions are excluded too — they run unattended under an admin's auth and have their own watchdog. Opt-in per user on My Page (`users.auto_resume`, default off: it runs unattended hours later); admin `autoResume*` keys gate it workspace-wide and tune grace/attempt/pending/stale limits. The demo fakes the whole loop on a `!limit` prefix
- **feat: keep a user's claude.ai 5-hour window open with a primer query (`7db52ae`)** — the window is not a wall clock: it opens on the first billed message and closes 5 hours later. Idle after a reset and that time is simply gone — sit out an hour and you get 4 usable hours, not 5. For users who opt in, a per-user scheduler probes the live window and, when none is open, sends one tiny throwaway query (cheap model, tools denied, hard timeout) to open it, then sleeps until that window's real `resets_at`. It is not a chat session: no `chat_sessions` row, no messages, nothing in the sidebar — just a short-lived CLI subprocess in the user's project dir, billed as one `usage` row so the cost stays visible. Same Claude-subscription gate as auto-resume. An unreadable probe retries on `windowPrimerRetryMs` rather than guessing, so a bad guess never spends a message for nothing. Opt-in on My Page (`users.prime_window`, default off — it spends quota) with the last prime time shown live over a `user:primed` socket event; admin `windowPrimer*` keys tune model/prompt/grace/retry

## v1.4.0 — 2026-08-03 (`5e7a59b`)

- **feat(web): collapsible left sidebar on desktop (`607e460`)** — the column can be hidden (persisted in localStorage). The existing header hamburger doubles as the expand control: always visible `<md` for the drawer, visible `≥md` only while collapsed
- **feat(search): workspace-wide unified search, `Ctrl/Cmd+K` (`37033e9`)** — one endpoint + one palette across every internal surface: private chats, shared room chats, DM/group messages, projects, LLM Wiki topics and their compiled/raw documents, PR review sessions, and the user directory.
  - Visibility reuses each feature's own gate instead of re-deriving it (`canViewChat` semantics, `rooms.isMember`, projects `canAccess`, `listReviewSessionsForUser`, dm membership) — DMs stay membership-only even for admins
  - Message matching flattens the stored content JSON so prose, tool names, tool inputs and tool outputs are all searchable
  - Clicking a hit lands where the sidebar would: the thread opens and scrolls to the matched message with a ring (folded `/clear`|`/compact` blocks auto-open); project and wiki-file hits open a FileExplorer straight on the file
  - Admin config: `searchEnabled` (hard-404s the API + hides the UI), `searchMaxPerType`, `searchFileMaxKB`, `searchScanMaxFiles`
- **feat(search): sort by type (default) or one newest/oldest timeline (`0b71232`)** — grouped stays the default (one section per surface, newest-first inside each). The two time modes flatten every surface into a single list, each row carrying its own type badge; undated hits (people, wiki documents) trail the list in both directions instead of pretending to be the oldest. **Oldest-first also needs the server to pick candidates from the other end**: the per-type cap would otherwise hand back each surface's newest rows for the client to reverse, so the genuinely oldest hits never left the DB. Added `?sort=newest|oldest`, honoured by every time-ordered collector before capping. The choice persists per browser; a row's snippet is constrained to the row width (items-start let a long path/CJK run size to content and overflow the palette on a phone)
- **docs: note the search sort options (`b5ca545`)**
- **feat(search): time-only sort + per-feature filter tabs (`e5f86ed`)** — the grouped-by-type mode is gone. Results are always one newest/oldest timeline; narrowing happens through filter chips: all · personal · rooms · DM · projects · LLM Wiki · PR reviews · people, each with its hit count, and only surfaces that actually matched get a chip. Tabs are per **feature**, not per hit type — a message inside a room files under "rooms", the same shape inside a wiki thread files under "LLM Wiki" — resolved from `nav.kind`. Every row keeps its type badge now that there are no section headers. Switching sort refetches, and a filter the new query has nothing for falls back to "all"
- **fix(search): scope candidate rows to the caller before the per-type cap (`1a976f9`)** — visibility was already correct (the in-memory `chats` map is the gate; an audit with a throwaway member account confirmed no cross-user leak), but the private-title collector ran its LIKE + LIMIT over every user's `chat_sessions` and only dropped invisible rows afterwards — so on a busy workspace a member's own matching chats could be pushed off the end of the limit by rows they can never see. Owner-scoped in SQL for members (admins keep the cross-user view they already have through `canViewChat`). The message prefilter gets the same escape hatch when someone's visible-session list is too large to bind as an IN list, so a huge account degrades to a wider scan instead of a SQLite parameter error
- **fix(search)!: never let an admin search another user's personal space (`87ccb9b`)** — `canViewChat`/`canAccess` let an admin open a single private thread or a user-scoped project directly, and search inherited that. **Opening one thread on request and grepping every user's private conversations at once are not the same permission**, so search no longer takes the admin shortcut:
  - private chats and wiki query threads: owner only, admins included (`visibleChats` loses its isAdmin branch, and the title query is owner-scoped in SQL)
  - user-scoped projects: owner only
  - messages: always prefiltered to the caller's own visible sessions, so an admin's query never reads another user's private messages off disk at all
  - DMs were already membership-only
  - Shared surfaces are untouched: rooms the caller belongs to, room-scoped projects, PR review sessions, the LLM Wiki knowledge base, the user directory. Single-item endpoints keep their existing gates — this narrows search only. Verified against the running container with a throwaway member account: the member's own private session and project are found by them (2 hits) and by the admin not at all (0 hits, also with `types=` forced)
- **feat(web): the logo returns to the landing screen, with search in its centre (`a943777`)**
- **fix(web): sidebar header no longer hides the language toggle behind the title (`f2fb154`)**
- **fix(web): move the language switch to the sidebar footer so the title fits (`a35a08d`)**
- **refactor(web): pick the language from a list instead of a two-way toggle (`965f4be`)** — a toggle only works while there are exactly two languages, so it was the wrong control for a UI that will gain more. `LangSelect` (native `<select>`, options from `LANGS`) replaces `LangToggle` in the sidebar footer and on the login page; `Lang` is derived from `LANGS` and `detect()` validates the stored/browser value against it instead of branching on `'ko'`/`'en'`; `toggleLang` is dropped and `lang.toggleTitle` becomes `lang.pickTitle`, which also stops baking the language list into a translated string. Adding a language is now one LANGS entry + its label + its dictionary (DICT is typed per-Lang, so tsc points at what's missing)
- **feat(web): keyboard shortcuts for the core actions, keyed per platform (`bd9b76a`)** — one table (`SHORTCUT_GROUPS`) drives both the global handler and the `?` cheat sheet, so a listed binding always works. Keys render the way each platform writes them (⇧⌘O on a Mac, Ctrl+Shift+O elsewhere). Mod+K / Mod+/ search, Mod+Shift+O new chat, Mod+B sidebar (drawer `<md`, column `≥md`), Mod+Shift+H home, Mod+Shift+L theme. `?` opens the cheat sheet only outside a text field, so typing one in a message never triggers it; an open dialog owns the keyboard. `Esc` in the composer interrupts the running turn once no menu is open. Buttons carry their shortcut in the tooltip; the hardcoded `search.shortcut` ('Ctrl+K') is dropped for `fmtKeys`
- **feat(web): the app owns right-click — its own context menu, Shift for the browser's (`733c583`)** — one window listener claims `contextmenu`, so a right-click anywhere lands on workspace actions: new chat, search, sidebar, theme, reload, plus clipboard rows when the click hit a field, a selection or a link. Surfaces with better items attach their own (sidebar rows, chat messages) and the global handler stands down for anything already default-prevented. Escape hatch: hold **Shift** while right-clicking — we never preventDefault then, so the browser's own menu comes through untouched (Firefox / VS Code convention). Listed in the shortcut sheet and the menu's own footer; admins can disable the whole thing with `customContextMenu`. Mod+B's sidebar logic moves into a shared `toggleSidebar()`, and the sidebar's rename/delete prompts into named handlers, so the hover buttons and the menu rows run the same code
- **chore: local compose rebuilds clean up after themselves (`94ce165`)** — `docker compose up -d --build` leaves the previous image dangling every time. Build + `docker image prune -f` are wrapped in one script and CLAUDE.md's release workflow points at it, so no session has to remember the cleanup step. Prune only touches dangling images: tagged versions, running containers, volumes and the build cache stay
- **feat: name a fresh chat after its topic, toggleable per user (`663b859`)** — a new private chat kept its placeholder title until someone renamed it. Once the first turn finishes, the server asks a cheap model for a one-line title and pushes it to every client over `session:title`. Only chats still carrying the placeholder are touched, so a name the user chose always wins; no auth or a failed/timed-out call falls back to truncating the first message. Off is one checkbox on My Page (`users.auto_title`), and admins can drop the whole thing with `autoTitleEnabled` — model, length cap and timeout are registry keys too. The static demo mirrors the flow with a canned title
- merge: `feat/auto-session-title` (`bca1272`)

## v1.3.1 — 2026-08-03 (`0f736c0`)

- **fix(privacy): master switch overrides and locks the per-channel toggles (`f1cfd57`)** — two problems with the panel as shipped. A bare checkbox next to "Usage telemetry" does not say whether ticking it sends the data or blocks it, and the master switch read as "block everything" while actually being an AND gate — so turning it on left nine live-looking checkboxes underneath that could each appear to undo it. Master is now an **OR override**: on = every channel blocked, per-channel keys ignored, and those rows render locked (greyed, disabled, effective state shown) with a line naming the switch holding them. Turn it off to choose channel by channel; with the master and every channel off, nothing is blocked and the inherited env is untouched. The lock is driven by a new `disabledWhen` field on `ConfigDef` rather than the view special-casing the privacy group, so the registry stays the single source of truth. Every label now reads "Block …" and the group carries a "checked = blocked" hint

## v1.3.0 — 2026-07-31 (`68e920c`)

- **feat(privacy): per-channel toggles for non-essential Anthropic egress (`1e62f66`)** — the single master switch stayed, but an operator who wants exactly one channel back (metrics into their own OTel collector, say) had to give up all of them. `privacy.ts` now describes each channel as data — key, env it pins, inherited vars it strips, settings it merges, and whether the umbrella variable covers it — and `privacyPlan()` resolves the master plus the nine per-channel keys into one plan the two spawn points apply. `on` is passed in rather than imported so the module stays DB-free and the self-check can drive it directly. `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC` is only emitted while every channel it covers is still on: it blocks telemetry, error reports, the survey and the updater wholesale, so setting it next to a deliberately re-enabled channel would have quietly overridden the operator. Master off still leaves the inherited env completely untouched. Descriptions live in i18n (ko + en) and name what leaves the machine, not just the env var

## v1.2.0 — 2026-07-31 (`de73205`)

- **docs(readme): recommended specs — CPU/RAM/disk/arch/network (`7d123d5`)**
- **docs(readme): emphasize the fully-local setup, refine specs (`b39d856`)** — a "fully local" section (`ANTHROPIC_BASE_URL` → LiteLLM/local model, no api.anthropic.com) and a note that local-LLM GPU/VRAM is separate from app specs
- **docs(readme): fully-local run stack — Ollama + LiteLLM + app (`1adfd4b`)** — copy-paste compose for offline operation; the app points at the local Anthropic-compatible gateway via LLM Provider (custom)
- **docs(readme): docker run variant for the fully-local stack (`fcb675c`)**
- **docs(readme): fix local LLM setup — direct native Anthropic endpoint (`8e84bff`)** — Ollama ≥0.14, vLLM, LM Studio and llama.cpp expose `/v1/messages` natively, so Claude Code connects directly via `ANTHROPIC_BASE_URL`. LiteLLM dropped from the default local stack (kept only as fallback)
- **feat(release): default to amd64, add `--arm` for occasional multi-arch (`0a97718`)** — arm64 emulated builds take ~20–30m on this host, so release builds amd64 by default; pass `-- --arm` to also publish linux/arm64
- **fix(docs): tilde number ranges rendered as strikethrough in GFM (`fe74580`)** — `4~8` / `~20~30` parse as `~strikethrough~` on GitHub. Use en dashes
- **docs(readme): re-record the demo GIF against the current UI (`c24608c`)** — the old hero GIF was from an early build. Re-captured from the current VITE_DEMO app: team room + presence, streaming turn, in-browser tool approval, tool run, Split into code-server. English UI, 1200px
- **docs(readme): add a section index (`b0ec001`)** — a top-level TOC after the hero in both READMEs, anchors verified against GitHub's rendered slugs
- **docs(readme): Docker ≥26 badge in the header (`2d3526a`)**
- **feat(privacy): block all non-essential Anthropic egress by default (`1e8f69f`)** — every agent turn spawns the Claude Code CLI, which by default also talks to Anthropic outside the inference request: usage telemetry, error reports, `/feedback` + `/bug` + `/share` (these upload the whole transcript incl. code), the session-quality survey and its transcript-upload follow-up, non-essential model calls, auto-updater pings, the WebFetch domain preflight (sends the hostname to api.anthropic.com), Artifact publishing, official-marketplace auto-install and OpenTelemetry export. New `server/src/claude/privacy.ts` holds every opt-out in one place and is applied at the two spawn points: `buildOptions()` (env pinned **last** so nothing upstream can reopen a channel, plus `skipWebFetchPreflight` via the SDK flag-settings layer, which is a setting rather than an env var) and the code-server container spec, so a `claude` run from the editor terminal is covered too. Inherited OTel/tracing endpoints and headers are stripped, not just overridden. Gated by `blockNonessentialTraffic` (default on); off leaves the inherited env untouched so a deliberate OTel collector still works. The inference request itself is never affected — LLM Provider = custom remains the way to remove that too

## v1.1.1 — 2026-07-31 (`6d2d8c5`)

- **docs(dockerhub): repo overview page + per-shell run commands (`5ea9258`)** — `DOCKERHUB.md` (Hub overview with GitHub link, bash/PowerShell/CMD run blocks), `scripts/hub-description.mjs` + `npm run hub:desc` to push the overview via the Hub API, README docker run split into bash/zsh, PowerShell and CMD variants
- **feat(release): multi-arch build via buildx (`d5b1956`)** — `release.mjs` uses `docker buildx build --platform … --push` and auto-creates a docker-container builder (`ccw-multi`) if missing. Override arches with `PLATFORMS`

## v1.1.0 — 2026-07-31 (`8d01636`)

- **feat(release): version + Docker Hub image publish pipeline (`8102e6a`)** — `scripts/release.mjs` builds & pushes `:version` / `:latest` / `:sha-<short>`; `npm run release[:patch|:minor|:major]` (npm version bumps + git tag); compose `APP_IMAGE` param pulls the published image; CLAUDE.md rule 3 + READMEs document the release step
- **feat(deploy): standalone `docker-compose.hub.yml` for clone-free runs (`ecfe492`)** — pull-only compose (no `build:`) so users run the published image with a single downloaded file
- **feat(codeserver): self-provision the network so a single `docker run` works (`7ee811c`)** — `ensureNetwork()` creates the code-server network and attaches the app container on boot. No-op under compose

---

## Early development — 2026-07-20 → 07-31

144 commits before the first version tag. The build followed the **P0–P5 stages** in section 14 of [DESIGN.md](DESIGN.md); axes the design never mentioned arrived after that.

### 07-20 — the P0–P5 skeleton (straight from the spec)

- **chore: scaffold monorepo, Docker deploy, build config (P0) (`b536aac`)** — compose, single image, socket mount, named volume
- **feat(db): SQLite/Drizzle schema, DDL init, path/settings/id utils (P0) (`f043752`)**
- **feat(auth): scrypt auth, revocable DB sessions, account provisioning (P0) (`6c155d3`)**
- **feat(claude): per-session SDK runner, streaming, `canUseTool` web permission bridge, global throttle + 429 backoff, usage tracking (P1) (`545efb6`)**
- **feat(rooms): shared rooms (owner/delegation), FIFO queue + cancel, Socket.IO fanout/presence (P4) (`47153ef`)**
- **feat(codeserver): dockerode spawn/reap, scoped volume-subpath mounts, in-app http+ws proxy (P2) (`6d8dcdf`)**
- **feat(plugins): two-class plugin manager (P3) (`f7c1a44`)** — common/personal, git + tarball, forced/preferred
- **feat(api): REST routes (sessions/rooms/projects/plugins/admin) + Fastify entrypoint (P0–P5) (`6e6e220`)**
- **feat(web): React SPA (P0–P5) (`4fc95f4`)** — chat/tool-cards/permission prompts, rooms, editor split, admin + plugins panels
- **feat(codeserver): remove a user's editor containers on logout (P2) (`b611365`)**
- **fix(codeserver): wait for code-server to bind `:8080` before returning the URL (`f6a0e5c`)** — avoids the iframe 502 race
- **fix(web): editor-only view rendered in a 0-width grid column, so the editor was blank (`de41ba9`)**
- **fix(codeserver): remove orphaned editor containers on boot (`0dcc963`)** — untracked survivors were never reaped
- **docs: polished OSS README (strengths / architecture mermaid / badges) + MIT LICENSE (`219f1d1`)**
- **docs: add the live demo GIF to the README (`85207d9`)** — chat · web tool approval · code-server split
- **feat(web): app icon (clay spark favicon.svg) + PWA manifest + theme-color (`5642b85`)**
- **feat(brand): original app icon (split-workspace mark) across favicon, in-app logos, README (`bfb51ba`)**
- **docs: i18n README — English (default) + Korean with a language switcher (`96a98c6`)**

### 07-21 — the LLM Wiki (a fourth entity the design never had)

- **feat(server): route OAuth tokens vs API keys, wrap local plugins, permission answer channel (`2b7d42a`)** — `sk-ant-oat*` via `CLAUDE_CODE_OAUTH_TOKEN`, `sk-ant-api*` via `ANTHROPIC_API_KEY`; plugins wrapped as `{type:'local',path}`; isolation roots / `additionalDirectories`. AskUserQuestion selection delivered back to Claude via deny+message (answer channel), emitting `permission:resolved`/`answered`. `.env.example` gains the bootstrap admin + key notes
- **fix(web): chat pane scroll + grid layout `minmax(0,1fr)` (`8519167`)**
- **feat(projects): create a project by git clone (`8f5324e`)** — `POST /api/projects` accepts an optional `gitUrl` → clone (execFile, no shell) instead of mkdir. Validates http(s)/git/ssh (blocks `file://`), derives the name from the URL, `GIT_TERMINAL_PROMPT=0` so private repos fail fast, cleans the partial dir on failure
- **feat(wiki): LLM Wiki tab — admin topics, per-user query threads, bulk + folder upload (`af18dd4`)** — `wiki_topics` table + `chat_sessions.wiki_topic_id` + compile status columns (guarded ALTER migrations), topic dirs and wiki cwd resolution (the session list excludes wiki threads), `routes/wiki.ts` (topic CRUD with admin-only create/delete, get-or-create per-user thread, staged upload = upload → deletable list → confirm/cancel, folder drag&drop with recursive all-depth paths, CLAUDE.md grounding), raised multipart `fieldNameSize`, staging reaped on boot, queries blocked while a topic compiles, plus the Wiki sidebar section, `WikiCreateModal` (drop zone, progress bar, staged list), `WikiBanner`, store/socket state and `api.uploadProgress`
- **feat(wiki): full compile pipeline — raw sources → synthesized articles + `_index` (`9bc2159`)** — `compileTopic()` runs Claude (acceptEdits + always-allow `canUseTool`) over `./raw/` to produce cross-linked `./wiki/` articles with confidence and a hierarchical `_index.md`. Auto-runs on create/add-file, manual recompile button, single-flight guard, empty/mock → instant done. Status (idle|compiling|done|error) + a live per-step heartbeat broadcast over `wiki:status`/`wiki:progress` so it never looks hung
- **feat(wiki): `raw/` immutable fence in compile + file-tree & single-file endpoints (`3c742b7`)** — the compile `canUseTool` denies Write/Edit under `raw/` (originals stay immutable even if the agent misbehaves), `wiki/` output only. `GET …/tree` → `{raw, wiki}` paths + sizes (no content), `GET …/file?dir=raw|wiki&path=` → one file's text
- **feat(wiki): tree file explorer for topic files, raw + compiled (`1367783`)** — a `WikiBanner` button opens a modal: raw/wiki toggle, collapsible folder tree built from relative paths, lazy per-file viewer. Shows all uploaded originals (incl. nested + binaries) so nothing looks lost after compile
- **fix(wiki): preserve unicode (Korean/NFD) folder names on upload (`14a8a1f`)** — `safeFile`'s `[가-힣]` whitelist matched only precomposed NFC syllables, so macOS-decomposed (NFD) Hangul jamo were stripped — a pure-Korean folder became `''` (inner files fell to the parent) and Korean+alnum lost the Korean part. Replaced with `safeSeg`: NFC-normalize + strip only path separators/control chars, keep all unicode. Verified nested Korean/mixed paths survive
- **feat(wiki): delete topic files on disk + sweep orphaned dirs at boot (`91460d1`)** — topic delete now rm's the topic dir (raw/ + wiki/), not just the DB row. `reapWikiOrphans()` removes any `/data/wiki/<id>` with no DB row at boot (cleaning leftovers from the old keep-files behaviour). The confirm dialog warns files are permanently deleted
- **feat(wiki): include images in compile (multimodal) + query grounding (`2e315ce`)** — images were not code-excluded, but the prompt never mentioned them so they were effectively ignored — and at query time grounding points at text `wiki/`, so `raw/` images were unreachable. Compile now reads images (.png/.jpg/.gif/.webp) via the multimodal Read tool and transcribes/describes diagrams + screenshots into the articles with source citation; grounding tells the query agent to open `raw/` images directly for visual questions. Verified: a red/blue test image was read and described (confidence high)
- **feat(wiki): image preview + markdown render toggle in the file explorer (`a89e08e`)** — `GET …/blob` streams raw bytes with an image content-type (same-origin cookie auth) for `<img>` preview; the markdown renderer is extracted to `lib/md.ts` (shared by chat + explorer); image files render as `<img>`, `.md` gets a rendered/raw toggle, other text stays raw
- **fix(md): proper block-level markdown renderer, chat + wiki preview (`03bae9a`)** — the old renderer turned every newline into `<br/>` (huge gaps) and had no hr/table/h4-6/strikethrough/task-list support. Rewritten as a block parser: headings 1–6, hr (`---`/`***`/`___`), blockquote, fenced + inline code, ul/ol (split on ordered switch) + task items, GFM tables, images, paragraphs with soft breaks (block margins, not `<br/>` spam). Escape-first so still XSS-safe; NUL-sentinel placeholders so digits/spaces in text are never corrupted; blockquote/list detection runs post-escape (matches `&gt;`). Verified across all syntaxes
- **feat(chat): copy buttons for answers and code blocks (`1e143de`)** — a message hover control copies an answer (assistant text blocks joined, tool cards excluded; user messages copy their text), and every markdown code block gets a copy button via one delegated click listener (content is `dangerouslySetInnerHTML`, so no React onClick), reading `pre code` textContent to copy the decoded source. The non-rendering ⧉ glyph replaced with 📋
- **feat(chat): project file explorer in the chat header (`dfb89f1`)** — a Files button on project chats opens a tree+preview modal like the wiki explorer; the wiki explorer's shared tree/preview is extracted into a generic `FileExplorer` that `WikiExplorer` now wraps. New endpoints `GET /api/projects/:id/tree|file|blob` (bloat dirs skipped, 5000-file cap, path-traversal guarded, binary/large files not previewed)

### 07-22 — per-user tokens · i18n · static demo · git commit/push

- **docs: add CLAUDE.md with workflow rules (`aed6b6e`)**
- **feat(token): encrypted token storage + per-author resolution layer (`d06bde9`)** — `secret-box` (AES-256-GCM at-rest), `users.claude_token_enc`/`_set_at` columns + idempotent migration, `claude-token` (set/clear/meta for user + admin common token, `resolveClaudeAuth`), config gains `forceMock` + `tokenEncSecret`; `anthropicApiKey` demoted to legacy fallback
- **feat(token): route each turn through its author's token + token APIs (`8310b93`)** — `buildOptions` injects the resolved per-author token and scrubs stray host keys; `runTurn`/`probeCommands`/wiki-compile resolve per author/creator; a per-turn mock when no token resolves (replacing the global `mockClaude`); `PUT/DELETE /api/auth/me/claude-token`; `/me` and `/login` expose token status; `POST /api/users` accepts `claudeToken`; admin common-token endpoints
- **feat(web): token registration UI, login nag, admin common token (`37cddf9`)** — `MyTokenModal` (self-service register/update/clear + nag variant), sidebar entry with an unregistered badge, a nag modal each login until a token is registered, AdminPanel common-token section + optional token on user creation, `.env.example` gains `TOKEN_ENC_SECRET` + token precedence docs
- merge: per-user Claude Code tokens, personal + shared-room per-author (`ee0a3d8`)
- **feat(plugins): detail (manifest + skills), file tree & update API (`bb43660`)** — `lib/filetree.ts` extracts `walkFiles`/`resolveUnder`/`IMG_CT` shared by projects + plugins; `GET …/detail` → plugin.json manifest + `skills/*/SKILL.md` frontmatter; `GET …/{tree,file,blob}`; `POST …/update` → git fetch+reset to remote HEAD (marketplace only); `canViewPlugin` (common visible to all, user-scoped to owner/admin)
- **feat(web): plugin detail modal — skills list, file tree, update (`4cc47f7`)**
- **feat(wiki): import an already-compiled wiki on topic create, skipping compile (`432ec46`)** — a "precompiled" option treats the uploaded folder as a finished wiki instead of raw sources: staged files are placed under `wiki/` (and `raw/` when a topic-export folder carries both), the topic is marked `compileStatus=done`, and the Claude compile step is skipped entirely. `mapPrecompiled()` normalizes the staged tree (strips the wrapper folder, routes `wiki/*`+`raw/*` for topic exports, else all → `wiki/`)
- **feat(web): i18n, Korean + English (`a42a7b1`)** — a self-contained module (`web/src/lib/i18n.ts`): lang state + localStorage persistence + `useSyncExternalStore` subscription, `t()`/`useT()`, ko/en dictionaries (~230 keys). Default language: localStorage → `navigator.language` → ko. `LangToggle` in the chat header and login page; all Korean UI strings externalized across 12 components + App/store/ui; `{name}` interpolation; `timeAgo` and store errors use the module `t()`
- **feat(web): move the language toggle to the sidebar top, global on all pages (`fbf6c2d`)** — visible on chat, plugins and admin panels; the redundant chat-header toggle is removed (login keeps its own pre-login one)
- **fix(web): keep the sidebar title on one line in English (`23476b6`)** — float `LangToggle` absolute top-right so the fixed 264px sidebar title no longer wraps when the toggle + text widen in EN
- merge: `origin/feat/i18n` (`c057788`) — Sidebar.tsx conflict resolved
- **docs: standing rules for i18n + README upkeep; document LLM Wiki & i18n (`b05471d`)** — CLAUDE.md rule 6 (all user-facing strings go through i18n, ko+en keys) and rule 7 (meaningful new features go in both READMEs)
- **docs(readme): refresh with per-user tokens, plugin detail, LLM Wiki, i18n (`7c3b6b1`)** — token precedence (personal → admin-common → env → MOCK) as a strength, feature section, `.env` note and checked-off roadmap item; structure section gains `src/wiki`, `src/auth`, `src/usage`, web i18n
- **feat(web): fold conversation history at `/clear` and `/compact` into stacked toggles (`d075c12`)** — each `/clear` or `/compact` user message closes a segment; the conversation above it collapses into a timestamped, per-command toggle (default folded), so folds stack as the two commands accumulate. Purely a render pass over the persisted message list — history is never lost, just tucked away
- **feat(web): cited-sources panel for LLM Wiki answers (`0e6d4e7`)** — a right-side panel per wiki thread listing the files each answer drew on (grouped wiki / raw). Sources come from the turn's Read tool calls plus `wiki/`//`raw/` paths the model names in prose. Mentions in the answer are wrapped as `<mark>` citations: hovering a source highlights its inline mentions and vice-versa; clicking previews the file in place. Written paths are approximate (the model collapses whitespace), so the preview resolves a cited path onto a real tree entry by normalized basename before fetching, and fails gracefully inline on a true miss
- **feat(web): static GitHub Pages demo mirroring the real app (`d300b00`)** — a backend-free demo build (`VITE_DEMO`) reuses every real component, store and style, swapping only the network layer so the demo looks identical and new UI shows up automatically. `web/src/demo` (fetch + XHR + socket.io mock: router, seeded in-memory db, simulated streaming turns with a web permission prompt) installed from `main.tsx` behind `import.meta.env.VITE_DEMO` (tree-shaken from normal builds), a `build:demo` script + Pages deploy workflow, favicon refs via `BASE_URL`, README demo badge + CLAUDE.md rule 8 + `web/src/demo/README.md`
- **feat(web): make the LLM Wiki sources panel resizable (`b0f9a4b`)** — the inline preview was cramped at a fixed width. A drag handle on the panel's left edge resizes it (clamped 300–1000px, persisted to localStorage); default widened to 360px, and the preview fills the wider panel keeping the side-by-side hover-highlight layout instead of a popup
- **fix(web): render `<aside>` blocks and relative images in wiki markdown (`30fb4c3`)** — Notion exports wrap callouts in `<aside>` and link images with relative, URL-encoded hrefs. `md()` escaped the tags (shown as literal "<aside>") and only rendered http(s) images, so both were broken. `md()` now unwraps `<aside>` wrappers and takes an optional `opts.img` resolver for non-http image srcs; file previews (sources panel + explorer) pass a resolver mapping a relative href onto the topic/project blob endpoint (resolved against the file's own directory; the server trims stray segment spaces)
- merge: `feat/wiki-sources-panel` (`fac5d3f`)
- **feat(demo): seed an LLM Wiki thread so the cited-sources panel demos (`e5aed82`)** — the demo's Payments Domain thread had a plain answer with no tool calls, so the new panel rendered empty. Seeded with Read calls on `wiki/_index.md`, `refunds.md`, `overview.md` plus inline citations, and an `_index.md` article — the panel now lists 3 wiki + 1 raw source, highlights in-text mentions on hover and previews each file on click, all in the mocked demo
- **docs: design spec for git commit/push + remote credential management (`304bbad`)**
- **feat(git): commit/push from chat + encrypted remote credentials (`a7418b8`)** — `git_credentials` table (per-user + admin-common, AES-GCM, host-keyed), user→common resolution by remote host, HTTPS PAT via a static `GIT_ASKPASS` helper (the secret only in the child env, never in the URL/reflog), project git endpoints (status / commit with file staging / push) and a clone credential picker, git author identity + push creds handed to the Claude subprocess so it can commit/push itself, plus the credential management modal (user) + admin section, the header Git panel, i18n ko/en, demo mocks and READMEs
- **fix(git): bind clone credential to URL host; commit whole renames (`2abdd2c`)** — clone is rejected when an explicit `credentialId`'s host differs from the repo URL host (**prevents exfiltrating a stored PAT to an attacker-controlled URL** — review: high); `gitCommit` expands a selected staged rename to include its origin path so `commit -- <new>` doesn't drop the staged deletion of the old path (review: medium); `parsePorcelainZ` retains the R/C origin token as `GitFile.orig`
- merge: `feat/git-commit-push` (`7053eca`)

### 07-23 — sanding down the git workflow

- **fix(git): credential hint clarifies the Bitbucket username, email for ATATT API tokens (`d4749fc`)**
- **feat(git): branch list (local/remote) + switch in the Git panel (`f6a5b99`)** — `gitBranches` (current + local + remote, `origin/HEAD` filtered), `gitCheckout` (DWIM: local switch or auto-track a remote-only branch), `GET /git/branches` + `POST /git/checkout`, a branch `<select>` with local/remote optgroups replacing the static badge, demo mock + i18n
- **docs: mention branch switching in the git feature (`4fccdee`)**
- **fix(git): list all remote branches — shallow clones were single-branch (`4a0cd5f`)** — clone with `--no-single-branch` so a depth-1 clone still fetches every branch tip (`--depth` alone implies `--single-branch`); `gitFetchRemotes()` widens the origin refspec to `*` and fetches, so pre-existing single-branch clones also surface all remote branches (called best-effort before listing); the panel loads branches without blocking the status view. Verified over real network (octocat/Hello-World): before = master only, after = master + test + octocat-patch-1
- **feat(git): delete a project from the project menu, files included (`f47160e`)** — `DELETE /api/projects/:id` also removes the working dir, but only if it resolves strictly inside the scope's projects root (path-escape guard), with access tightened via `canAccess`. Fixes the re-clone name conflict from leftover files. The store unindexes + clears the current project if selected; each project row gets a 🗑 with confirm (pointerdown stopPropagation so it doesn't trigger project-select)
- **fix(git): separate project select vs delete in the project menu (`f13a849`)** — Radix `DM.Item`'s `onSelect` fired even when clicking the trash icon (stopPropagation didn't stop it), so delete also switched the project. The menu is now controlled (open state) and each row renders as plain select + delete buttons — delete no longer triggers a switch and keeps the menu open
- **feat(session): rename a private session from the sidebar (`532fba8`)** — `store.renameSession` → `PATCH /api/sessions/:id { title }`, updating the list + current title in place, with a ✎ button on each private session
- **feat(server): optional TLS so the PWA installs off-localhost (`64e4d0e`)** — PWA "Install as app" needs a secure context; browsers exempt localhost but not `http://<ip>`, so install worked at localhost:3000 but never on a server accessed by IP. Serve HTTPS when `TLS_KEY`/`TLS_CERT` point at a browser-trusted cert (socket.io + the `/cs` proxy ride the same server, so one listener covers all); empty = plain HTTP. Compose env passthrough + read-only `./certs` mount, docs + `.env.example`, certs gitignored
- **feat(git): show which credential a repo's push/commit resolves to (`8a159b1`)** — the panel only signalled has-credential as a bare check/warn mark. It now surfaces the actual resolved credential (source: yours vs shared, provider, host, username) and the commit identity, so auth failures like a rejected or expired PAT are diagnosable at a glance. `resolveGitCredMeta()` returns meta only — the token is never sent
- **feat(git): full clone instead of shallow depth 1 (`895ac7d`)** — project clone fetches complete history + all branches so `git log`/`blame` work. Also drops `--depth 1` from the ref-refresh fetch, which would otherwise re-shallow a full clone on every branch listing
- **feat(git): optional branch input when cloning a project (`bf2d2aa`)** — the form takes a branch; the backend validates the ref name (safe chars, no leading dash to block `--arg` injection) and passes it to `git clone --branch`. Empty = repo default

### 07-24 — PR review sessions (DESIGN §16)

- **feat(review): PR-review session backend — clone, poll, local merge (`b6a84fb`)** — a new admin-created session type parallel to private/room/wiki: `review_repos` (watched remote) + `review_sessions` (one per open PR) schema, GitHub/GitLab/Bitbucket Cloud PR/MR listing over REST, a manager doing full clone, host polling, per-PR git worktree and local no-ff merge (conflicts left in-tree for review) plus author→local-user matching, `/api/review/{repos,sessions}` (admin create/poll/delete; author read-only), review turns running in the PR worktree cwd (async `cwdFor`), admin write / PR-author read-only gating + `review:changed` broadcast in io, `kind=review` gating in sessions, a `REVIEW_POLL_MS` poller + manual refresh, boot orphan reaper
- **feat(review): PR-review UI — sidebar section, header controls, read-only (`6572ba3`)** — store state for repos/sessions plus `openReview`, add/delete/poll repo, local-merge and `review:changed` live refresh; a sidebar "Code review" section (admins see watched repos with nested PR sessions + poll/delete/add-repo modal, members see their read-only PRs); a chat review header (PR link, base←head, merge state, admin local-merge button, read-only badge) with the composer/model/mode locked for a read-only PR author
- **feat(demo): mirror the PR-review section in the static demo (`88630ac`)** — seeds a watched repo (acme/webapp) + two PR review sessions with a merged/build/review conversation, and mocks `/api/review/*`
- **docs(review): document PR-review sessions — README ko/en, DESIGN, .env (`48e061e`)** — DESIGN §16 covers the polling model, host adapters, worktree local merge, admin/author-readonly visibility and extension seams
- **fix(review): HTTP timeout on PR polling; document the author-match ceiling (`80b802c`)** — `AbortSignal.timeout(20s)` on host API fetch so a hung host can't stall `createRepo` or wedge a repo's poll lock (the `finally` now always runs → the lock always clears); a note on `matchAuthor` recording the trusted-team username-match assumption and its upgrade path (admin host-login→user mapping / verified email). Addresses two MEDIUM findings from adversarial review (0 CRITICAL/HIGH)

### 07-27 — the automatic review pipeline · sandbox · usage meter · mobile

- **feat(review): automatic pipeline — merge → build/run → review → verdict (`96c38b9`)** — on PR detection (`REVIEW_AUTO`, default on) the server runs the whole flow with no chat: local merge, then an **unattended** agent turn (review sessions auto-allow tools via `makeAutoAllow`; the class-1 fence still applies) that builds/runs, detects bugs, reviews the diff and emits `VERDICT: MERGE_SAFE|DO_NOT_MERGE`; `runTurn`'s `onDone` callback (threaded through the FIFO queue) parses the verdict + summary into `review_sessions`. A merge conflict → verdict=conflict, skipping build/review. `approvePr()` is the explicit admin action that merges the PR **on the remote** via the host API. Routes `POST /sessions/:id/auto|approve`; config `REVIEW_AUTO`
- **feat(review): auto-pipeline UI — verdict + re-run + remote merge (`c91e30e`)** — verdict/summary on review meta, `autoReviewRun` + `approveReview` actions, `review:changed` syncing the open session's verdict, a header VERDICT badge (running/merge-safe/hold/conflict) with summary tooltip, admin re-run and remote-merge buttons (with confirm), a sidebar badge, i18n and demo mocks
- **docs(review): auto pipeline + remote-merge (`eca05ec`)**
- **fix(review): withhold the git PAT from auto-turns; guard re-entrancy (`1aa4d54`)** — addresses adversarial-review findings (1 CRITICAL, 1 HIGH, 1 MEDIUM):
  - **CRITICAL**: review turns build/run PR-controlled code with Bash auto-allowed, so the merge-capable git credential is no longer injected (`buildGitEnv` skipped for `kind=review`). Review never pushes; the remote merge uses the host API
  - **HIGH**: `autoReview()` holds a per-review in-flight guard from local merge through the turn's `onDone`, so a re-run can't `git reset`/merge the worktree under a live turn (and can't race the verdict). The re-run button is disabled while verdict=running
  - **MEDIUM**: the verdict is an advisory LLM opinion, steerable by PR content — the approve confirm now tells the admin to read the diff first
  - The residual ceiling (auto-run executes PR code; Claude token in env; use `REVIEW_AUTO=0` for untrusted repos; sandbox = upgrade path) is documented in `makeAutoAllow` and the READMEs' security posture
- **fix(review): re-review on new PR commits; deliver turns to non-live clients (`78c7f2c`)** — (1) `pollRepo` detects a changed PR head SHA on an existing review session, resets the verdict, posts a note and re-runs the pipeline (the re-entrancy guard prevents overlap). (2) "History missing when not viewing": messages were always persisted server-side (verified — unattended auto-review conversations reload from DB); the gap was live delivery to a client not subscribed during the turn, so the server tracks the in-flight turn's partial blocks and replays them in the `session:join` ack, and the client re-joins the open session and re-fetches its messages on socket (re)connect
- merge: PR #2 `feat/pr-review-auto` (`fbd2d10`)
- **fix(review): clear stale live on reconnect; queue re-review on mid-run push (`308c676`)** — (A) `applyJoinState` always sets `live` (null when no in-flight turn), so a reconnect after a turn finished clears any stale live blocks instead of leaving a ghost/duplicate LiveView. (B) A push whose head lands while the pipeline is already running is no longer silently dropped by the re-entrancy guard: it's recorded in `rerunPending` and re-reviewed when the in-flight run finishes, so the new commit gets a fresh verdict and the "re-reviewing" note matches actual behaviour
- **fix(review): auto-review runs fresh, never resumes the prior review (`1601682`)** — root cause of "re-review didn't look at the new commit": `autoReview` enqueued its turn with the session's stored `claudeSessionId`, so the re-review **resumed** the first review's conversation. The model saw its own prior verdict, decided it was the "same task", and re-submitted the stale findings after only a quick typecheck/build — never re-reading the updated worktree (which did contain the new head; db head_sha == pushed SHA). Fix: clear `chat_sessions.claude_session_id` before each auto-review turn so every run reviews the current merged worktree from scratch
- **fix(review): watchdog so a hung auto-review self-heals (`3c630de`)** — a re-review turn hung (no claude subprocess, zero output) and left the review wedged: verdict stuck 'running' and the `autoRunning` guard never released, so it couldn't be re-run without a restart. The root cause is a transient SDK/turn stall, so the fix is a bound: a `REVIEW_TURN_TIMEOUT_MS` (default 10m) watchdog per auto-review turn aborts the turn, sets verdict=error, posts a note and releases the guard; a `settled` flag makes `done()`/`onDone`/watchdog idempotent
- **fix(review): the watchdog no longer early-releases the worktree guard (`8028893`)** — code-review finding on PR #3: on a timeout the guard was released immediately after `interruptTurn` (async abort), so a queued re-review's `git reset --hard`/merge could race the still-terminating turn on the same per-PR worktree. The watchdog now only aborts the turn and records the verdict; guard release and the queued re-review happen exclusively in the turn's `onDone` (fired by the abort's teardown, after the subprocess has exited) or on a pre-turn early exit. `setFinal()` makes the verdict write idempotent; `done()`'s Set.delete guard fires the rerun at most once
- **feat(review): run PR build/test in an isolated sandbox container (`768121e`)** — review turns no longer execute untrusted PR build/test code in the app container (which mounts the Docker socket ≈ host root):
  - `review/sandbox.ts` — a per-PR locked-down sibling container (worktree-only volume-subpath mount, **no docker socket**, CapDrop ALL, no-new-privileges, memory/pid limits, image pulled on demand), exposing an in-process MCP `run` tool (`mcp__sandbox__run`) that execs inside it
  - Review turns deny host `Bash` (`disallowedTools`) and get the sandbox `run` tool, so PR code can only run isolated. Falls back to host exec when Docker isn't available (trusted-team ceiling). The sandbox is removed at turn end; orphans reaped at boot
  - `config-layering` forwards `mcpServers`/`disallowedTools` to SDK options; `autoPrompt` builds/runs via the sandbox tool, and stacks the image can't build (.NET Framework / Windows-only, missing toolchain) get static review only with the verdict saying "build not run". `REVIEW_SANDBOX_IMAGE`/`_MEM_MB`/`_EXEC_TIMEOUT_MS`
- **fix(review): the sandbox mounts the repo dir at its real path so git works (`033afd6`)** — the worktree's `.git` file references the main clone's gitdir by absolute path, so mounting only the worktree broke `git diff`/`git log` in the sandbox (and the review agent runs git there now that host Bash is denied). Mount the whole `reviews/<id>` dir at its real absolute path (subpath-scoped) with cwd = the worktree; git metadata resolves
- merge: PR #3 `fix/review-repoll-and-live-delivery` (`c876330`)
- **fix(review): auto-review must build via the injected sandbox tool, not guess the env (`0dedf93`)** — the prompt let the agent judge build-availability by host inspection (looking for a `docker` CLI) and prematurely declare "build not run (environment constraint)". The sandbox is exposed as the `mcp__sandbox__run` tool via dockerode over the socket — **its presence in the tool list, not any host command, is the only signal**. Step 1 was rewritten so the agent always builds inside that tool when present, and only claims an env constraint after actually attempting the build
- **feat(review): post the finished auto-review back as a PR comment (`60df2ce`)** — when a turn produces a verdict, publish the verdict label + summary + review body onto the PR itself (GitHub issue comment / GitLab MR note / Bitbucket PR comment) via the same merge-capable credential. `postComment()` for all three hosts; `postReviewComment()` is called from the enqueueTurn `onDone` **only when THIS turn produced the verdict** (`setFinal` now returns a boolean), so a watchdog-timed-out partial review is not published. Best-effort — failures are recorded as a system note and never break the pipeline. `REVIEW_COMMENT` (default on) keeps reviews internal when 0
- merge: PR #4 `feat/review-pr-comment` (`0b183e0`)
- **feat(usage): show session context window + claude.ai plan limits in the chat header (`123312f`)** — a usage popover mirroring the CLI's `/usage` view: per-session context-window fill (`getContextUsage`) and claude.ai plan rate limits (5h / weekly / per-model) with reset countdowns (`usage_EXPERIMENTAL` SDK control call, unavailable for API-key sessions). Server-side `probeUsage()` reuses the `probeCommands` short-lived-query trick (resume the session, ask the CLI control channel, abort) with a 15s TTL cache, exposed at `GET /api/sessions/:id/usage`; frontend `UsagePill` (Radix popover) + i18n + static-demo mock
- **fix(usage): key the usage cache per requester; clear the stale pill on session switch (`e40b0ce`)**
- merge: PR #5 `feat/session-usage-meter` (`7f5f6ed`)
- **feat(mobile): responsive layout across the whole web UI (`b6e8e22`)** — the sidebar becomes an off-canvas drawer below md (768px), toggled by a hamburger in every top bar (backdrop + close button; navigation and panel switches auto-close it via `sidebarOpen`); the Shell grid is static two-column ≥md and single column with the drawer <md; on phones chat-only is forced (split view + code-server iframe are unusable at that width, the wiki sources panel is hidden but inline citations stay) with the header wrapping its pills and hiding the view-mode segment; the FileExplorer modal stacks tree/preview vertically <md; login, plugin install forms, git-credential and admin user grids and the admin usage table are made fluid/scrollable; side padding is reduced on the chat stream/composer/banner; new `useIsMobile` hook + `MobileMenuButton`
- merge: `feat/mobile-ui` (`b6a06e6`)
- **docs(claude): require responsive/mobile UI for all frontend changes (`6ed6f9d`)** — work-rule 9: no horizontal body scroll, `md:` branching over fixed grids, the drawer + `MobileMenuButton` + `useIsMobile` patterns, hide phone-useless views, verify at a mobile viewport

### 07-28 — room chat split out · interrupt · `@` autocomplete · the admin config registry

- **docs(spec): room chat vs claude-instruction distinction design (`83bba34`)**
- **feat(rooms): separate team chat from Claude instructions (`422ef8c`)** — a composer mode toggle (chat / Claude, default chat, sticky per room). Chat messages broadcast + persist only — no Claude turn. Typing `@claude`/`@클로드` flips to instruct mode. An optional "include chat" injects team chat accrued since the last turn as prompt context; a badge marks Claude-directed messages. `messages.chat` column (+ idempotent migration), `chat:send` extended with chat/includeChat, `postChat()` for turnless broadcast
- **fix(rooms): chat messages are delete-only; include same-ms chat in context (`f7f5368`)** — review follow-ups: the edit affordance is hidden for `chat=1` messages, because editing re-emitted `chat:send` without the chat flag, **firing an unintended Claude turn AND truncating all later room history** via /edit. Chat is casual — delete-only, no regenerate. The `includeChat` boundary goes `gt` → `gte` so a team-chat message written in the same millisecond as the last instruction isn't permanently dropped from context (the boundary row is `chat=0`, excluded by the `chat=1` filter → no re-injection)
- **fix(session): stop/interrupt actually halts the running turn (`6ec7012`)** — interrupt was wired end to end but relied solely on `abortController`, which only closes the CLI's stdin and waits ~2s for the graceful path — so the model kept streaming and "stop" felt dead. `runReal`'s loop also never checked the abort signal (unlike `runMock`), and a stop during rate-limit backoff slept out the full timer. `interruptTurn` now fires the SDK's control-channel `query.interrupt()` for an immediate turn-stop, with `abort()` kept as the subprocess-teardown fallback; `runReal` exposes its Query handle (`onQuery`) and breaks the stream loop the instant abort fires; a post-loop guard routes clean interrupt exits to the aborted-turn path so the partial is saved as interrupted; `withRateLimitRetry` takes an AbortSignal and cuts the backoff sleep short. Verified against the live SDK: `q.interrupt()` resolves in ~11ms with 0 tokens streamed after, generator fully stops in ~0.6s
- **feat(chat): `@` file/folder reference autocomplete in the composer (`6bc9a74`)** — typing `@` in any project-backed chat opens a fuzzy-search preview menu of files and folders (folders derived from the flat tree endpoint), mirroring the `/` command palette. Selecting one inserts an `@path` reference; Enter/Tab/Arrow nav + Escape to dismiss. Works mid-text and coexists with the room `@claude` mention. Reuses `GET /api/projects/:id/tree`; i18n ko/en; verified desktop + 375px
- **fix(chat): scroll the highlighted command/`@` row into view on keyboard nav (`c3e950c`)** — arrow-key navigation moved the selection but never scrolled the list, so the highlight could disappear below the fold. A module-scope callback ref (`scrollIntoView block:'nearest'`) on the highlighted row, applied to both the slash palette and the `@` picker; the demo project tree is enriched so the menu is long enough to actually scroll
- **fix(session): stop/cancel were dead — the `ack?.()` short-circuit skipped the call (`daf99ab`)** — the `chat:interrupt` and `chat:cancel` handlers put their side-effecting call *inside* the optional-chained ack: `ack?.({ ok: interruptTurn(p.sessionId) })`. Clients emit these events with no acknowledgement callback, so `ack` is undefined — and with optional chaining, `ack?.(arg)` short-circuits and **never evaluates `arg`**, so `interruptTurn()`/`cancelQueued()` were never invoked. That is why the earlier interrupt/abort logic looked correct but did nothing in production. The call is hoisted to its own statement and the result acked separately
- **feat(review): skip merge/build/run for docs-only PRs (`0010237`)** — read the PR's changed files before the pipeline: if they're all non-source (Markdown/text/images/LICENSE), skip the local merge + sandbox build/run and mark the PR MERGE_SAFE with a note. Unknown files count as source so real code PRs always get the full pipeline
- **feat(config): admin-managed settings registry for all runtime config (`b56e82b`)** — a single registry (`server/src/lib/config-registry.ts`) is the source of truth for every operational knob, resolving DB override → env → default. Runtime consumers read live via `cfg.int/str/bool`, so admin edits apply without a restart (turn-cap semaphore, review poller, code-server reaper re-arm via `applyLive` hooks); env vars now only seed defaults. New admin API `GET/PUT/DELETE /api/admin/config` (grouped, typed, secrets masked), public `GET /api/config` driving the model dropdown, a grouped live-editable AdminPanel Configuration UI (restart badge, reset), and migration of env + previously-hardcoded constants (git/provider timeouts, sandbox limits, session TTL, retry/backoff, usage probe, model default) into the registry. Infra (port/dataDir/TLS/docker) + secrets shown read-only
- merge: `feat/admin-config-registry` (`a4dd6f7`)
- **feat(config): friendly labels/descriptions, object editor, image pull, restart (`0ed0ba8`)** — (1) per-key displayName + description from i18n (`cfg.<key>` / `cfgDesc.<key>`, ko+en, graceful fallback to the raw key); (2) a structured editor for object/array JSON settings (the model map) with key/value rows plus array and raw-JSON fallbacks; (3) Docker image settings (code-server / review sandbox) get a presence check + pull/update, allowlisted to registry image values (`POST /api/admin/image/inspect|pull`); (4) a Restart button + "restart needed" banner (`POST /api/admin/restart` runs `process.exit`; the docker restart policy brings it back). Adds `server/src/lib/docker-images.ts`
- merge: `feat/admin-config-ux` (`a19c330`)
- **feat(admin): collapse config groups into dropdowns (`bea431f`)** — each category (Claude/turns, review, git, code-server, auth, server, infra, secrets) becomes a collapsed native `<details>` with a caret + item count instead of always-expanded cards. Zero JS, accessible, works on mobile

### 07-29 — local session import, plus ten feature groups

- **docs(import): design spec for local session import (`0eae20a`)** — project + `~/.claude` session files
- **docs(import): implementation plan (`eb335df`)**
- **feat(import): pure session-import module (encode/rewrite/backfill) + staging paths (`c83b85e`)**
- **feat(import): staging + confirm endpoints (`b793f40`)** — `POST/DELETE /api/import/staging/:sid` (files + slot whitelist + cancel), `GET …/sessions` (discover local sessions), `POST /api/import/sessions` (place project + cwd-rewritten jsonl + backfill), `reapImportStaging()` boot cleanup. The server computes the destination slug via `encodeSlug(path.resolve(dest))` — **client paths are never trusted**. `requireAuth` on all; `validSid`; `safeRelPath`
- **feat(import): store `importSessions` action + i18n ko/en (`f8a3a86`)**
- **feat(import): `ImportSessionModal` — gitignore tree + `.claude` guide + session picker (`c1c93cb`)** — a multi-step modal wired into the sidebar: project (pick/drop the folder, collected not uploaded; the root `.gitignore` seeds default checks), tree (a gitignore/.git-aware checkbox tree with directory cascade, `CLAUDE.md` and `.claude/*` force-checked + locked, uploading checked files to slot=project), claude (auto-open `.claude` folder picker + slug-encoding guide, skip supported, upload to slot=claude then list sessions), sessions (select-all checkbox list + project name, confirm → `importSessions`). Cancel/close discards staging
- **feat(import): demo mocks (`68e217a`)**
- **docs(import): README feature entry, en/ko (`e08436c`)**
- merge: `feat/local-session-import` (`35e5497`)
- **feat(import): collapsible directory rows in the upload file tree (`f18bef8`)**
- **feat(tree): expand-all / collapse-all in FileExplorer + the import tree (`0f899dc`)**
- **fix(import): per-file upload + two-bar progress; drop the auto-open picker; macOS hidden-folder hint (`f44daa8`)** — upload one file per request instead of one giant multipart request, fixing 'Payload Too Large' on big `~/.claude` session folders (a single transcript can be 20MB+, the whole slug dir tens of MB). A shared `<UploadProgress>` (overall bar by bytes + file counter, current-file bar with %) applied to session import and wiki staging uploads. The auto-open `.claude` picker is removed (it hid the guide popup on macOS), the guide shows the macOS reveal-hidden-folders shortcut (Cmd+Shift+.), and `uploadMaxMB` default goes 50 → 200
- **feat(admin): session-import feature flag + guideline to extract settings (`c675112`)** — new `sessionImportEnabled` (config-registry, group 'features', live toggle); `publicConfig` exposes it, the import routes gate on it (403 when off), the sidebar hides the button when disabled. i18n `cfg`/`cfgDesc`/`cfgGroup.features`. **CLAUDE.md rule 10**: new features must push tunable constants / feature flags into the config-registry `DEFS`, read live via `cfg.*`, and gate server-side too
- **docs(claude): no automatic merge to main from a branch without an explicit instruction (`9a27d4c`)**
- **feat(review): per-repo sandbox build image with a global fallback (`1aba1cd`)** — PR review previously ran every project in one global sandbox image (node:20-bookworm), so Python/Rust/etc. repos had no matching toolchain. An optional per-repo `sandbox_image` (nullable) is chosen at registration; `ensureSandbox` reads it and falls back to the global `reviewSandboxImage` when blank. No behaviour change for existing repos
- **feat(review): edit registered review repos — name/base/image/credential (`2dbfb3a`)** — `PATCH /api/review/repos/:id` + `updateRepo()` for in-place edits of the non-destructive fields. `gitUrl`/provider/host stay immutable (a different repo = delete + re-add); `credentialId` re-validates host binding + scope like `createRepo`; blank base/image clears to null. A pencil action on each repo row opens an edit modal (credential defaults to "keep current")
- merge: PR #6 `feat/per-repo-review-image` (`5e6455e`)
- **fix(review): self-heal interrupted auto-reviews — timeout retry + boot recovery (`0eeba85`)** — root cause of a review stalling mid-way: the watchdog killed a **still-working** turn at the 10-minute default and marked it verdict=error with no path back (polling only re-triggers on a new PR / moved head), so a healthy build+run+review that simply needed more time was stranded.
  - `reviewTurnTimeoutMs` default 10m → 30m (sandbox exec alone allows 5m/command); still admin-tunable up to 2h
  - New `reviewMaxRetries` (default 2): on a watchdog timeout, keep the review 'running' and auto-retry up to N times (fired from `done()` at real turn teardown) before a terminal error
  - Boot recovery: `recoverInterruptedReviews()` re-queues every open review left at verdict='running' by a process restart (the in-memory guard/watchdog/subprocess all died with the old process; polling never revives it). When `reviewAuto` is off it marks them interrupted instead
  - Await sandbox teardown before `onDone`: `removeSandbox` force-removes the build container (killing any build still writing the bind-mounted worktree) BEFORE a re-run's `git reset --hard`/merge touch that worktree — closing a retry-path race
  - `forgetReview()` clears `autoRunning`/`rerunPending`/attempts when a review/repo is deleted mid-pipeline, so the in-memory sets don't leak dead ids
  - Findings surfaced by an adversarial concurrency-verification pass
- **chore: ignore `bash.exe.stackdump` crash-dump junk (`698d18b`)**
- **feat(admin): split the admin panel into tabs — overview/users/providers/usage/config (`063e0eb`)** — the single-scroll panel becomes tabbed; a `TABS` array makes future tabs a one-line add. Pure frontend reorg, no behaviour/API change
- **feat(mypage): add My Page — avatar/token/git creds/projects (`9b00a83`)** — a per-user settings page consolidating profile image upload, Claude token, git credentials and personal project management into a new 'me' panel, moving the token + git-cred entry points out of the sidebar footer into a clickable profile row; avatar column on users, streamed via an authed route. **Security**: streaming-layer size cap (413), mime + magic-byte validation, mime-derived on-disk filename (traversal-safe), `safeName` rejects all-dot project names, nosniff header
- **feat(effort): per-session model reasoning-effort selector (`b36bc11`)** — an effort pill (low/medium/high/xhigh/max) next to the model selector, wired to the SDK `Options.effort`. New `chat_sessions.effort` column, `defaultEffort` admin config, PATCH validation, demo mock. Unsupported models downgrade silently
- **feat(chat): attach files + paste clipboard screenshots into prompts (`d8301b3`)** — upload files/images to a per-session `.attachments` dir inside allowed roots; absolute paths are prepended to the turn prompt so the agent Reads them (images render visually). The composer gets an attach button, paste handler, drag-drop and thumbnail chips; the transcript renders attachment chips. **Security**: streaming-layer size cap, count cap, atomic exclusive writes, basename sanitizer (traversal/RLO/ADS/Windows-reserved), room GET membership gate, nosniff. New config `attachmentMaxMB`/`Count`
- **feat(admin): host-Docker resource cleanup — scan/per-resource/full-reset (`6cd1ddd`)** — a Resources tab with a read-only inventory scan (ccw containers, referenced + dangling images, orphan review/attachment dirs, orphan DB rows) and cleanup actions. Full-reset removes spawned containers + dangling images + genuine orphans only — **never user data** (a module-load assertion enforces this). Safety: `rmSync` path-contained under the data root, dangling-only image prune, orphans re-derived server-side per action, a 10-minute clone grace guard, admin + `resourceCleanupEnabled` gate, double-confirm on full-reset
- **feat(provider): LLM provider override — bedrock/vertex/custom base URL (`11cec38`)** — per-user + admin-common provider profiles that build the turn's subprocess env for Anthropic/Bedrock/Vertex/custom-base-URL. Custom base URL is the path for OpenAI/ChatGPT/local LLMs via an Anthropic-compatible translating proxy. The default Claude-token path is unchanged when no provider is set. Secrets AES-GCM encrypted, never returned/logged. `buildOptions` clears **all** provider env vars before applying so host-global vars can't leak into default/mock turns. Routes gated by `requireAdmin`/`requireAuth` + `llmProvidersEnabled`
- **feat(requests): member request → admin approval workflow (`b962d99`)** — a generic approval framework with an action registry (add a requestable admin action in one place). Members submit typed requests (common_project / wiki_topic / role_upgrade) with a reason; admins approve/reject from a new tab; approval executes the action via the reused create functions. **Authz**: admin-only decide, `role_upgrade` promotes only the requester (no payload target), an atomic `WHERE status='pending'` claim = execute-at-most-once, members see only their own. Gated by `approvalsEnabled`
- **feat(admin): live activity/process manager tab (`561e470`)** — a live task-manager over running Claude turns, queued messages, code-server editor + review-sandbox containers, and running review pipelines, each with a per-row control (interrupt/cancel/kill). The read-only scan degrades gracefully without Docker and auto-polls while open (`processPollMs`), reusing `interruptTurn`/`cancelQueued`/`killEditor`/`killSandbox`
- **feat(dm): simple DM + group chat channels, promotable to a room (`74bb92b`)** — lightweight human-only messaging (no Claude): 1:1 DMs and group channels over Socket.IO, usable by all users. A sidebar Messages section + `DmView` with unread badges; an admin can promote a group channel to a common project room. Membership-gated on every read/write/socket path; promote is admin-only; socket payloads string-coerced (no crash-DoS); message length capped; gated by `dmEnabled`
- **feat(ui): unified hand-made SVG icon set, replacing standalone chrome emoji (`ec5d444`)** — `web/src/lib/icons.tsx`: 43 Feather/Lucide-style outline icons from one Svg wrapper (currentColor, 1.75 stroke, size prop, title/aria), replacing all standalone chrome glyphs (menu/back/close/chevrons/refresh/trash/edit/attach/status dots/Claude sparkle/…) across 16 components. Content/doc/demo emoji left intact
- **feat(ui): de-emojify i18n labels, render SVG icons at call sites (`21dc2af`)** — strips decorative emoji still embedded in i18n string values (admin/plugins/mypage titles, mode + verdict + tool-status labels) and renders the matching icon beside the unchanged text. `MODES`/`VERDICT_UI`/`ToolCard` maps now carry an Icon; Modal title widened to ReactNode; 5 icons added (user/crown/shield/bolt/link). Only genuine content symbols (→, ＋, ⌘⇧) remain — the emoji→unified-SVG conversion is complete

### 07-30 — closing the request flow

- **feat(requests): common-project creation is request-gated with real-form parity (`dfbc5ae`)** — members can no longer create a common (shared) project directly; the shared `ProjectCreateForm` routes member+common submissions through the approval flow instead of `POST /api/projects` (which still 403s scope=common for non-admins). The request carries the **same fields as the real create UI** (name + git clone URL + branch + credential picker); on approval the `common_project` action runs the actual clone as the requester. Extracted `createProject`/`validateProjectInput` (shared by route + action) preserve the credential ownership + host-match checks at both submit and execute. The form is reused in the Chat project menu (personal/common toggle) and My Page
- merge: workspace expansion — 10 feature groups + the common-project request flow (`9397a48`)
- **docs(readme): captured feature-tour gallery, 23 screenshots (`c4c2f0f`)** — `docs/screenshots/*.png` captured from the static demo (real UI, MOCK mode) covering every feature: chat/tool-cards, web permission prompt, usage meter, slash + `@` menus, rooms + delegation, DM/group, git panel, split editor, LLM Wiki + sources, PR review verdict, My Page, all admin tabs, plugins, i18n (ko), mobile/PWA. A "Feature tour" section is inserted into both READMEs; `main.tsx` exposes the zustand store on window in the demo build only (tree-shaken from prod) so screenshot/e2e tooling can drive views deterministically

### 07-31 — final polish before the first release

- **fix(login): prevent the language toggle firing on Enter-to-submit (`f182546`)** — `LangToggle` lacked a type and defaulted to submit. As the first submit button in the login form, pressing Enter in an input triggered it (toggling language) instead of the login button. `type="button"` added
- **feat(chat): click an image attachment to open a full-size lightbox (`fe30ac2`)** — a Radix Dialog overlay (Esc + focus trap free); backdrop/image click closes. Applies in the composer and transcript thumbnails
- **fix(chat): ignore Enter-to-send while the IME is composing (`90954fd`)** — pressing Enter to send while a Hangul syllable is still composing fired the submit and cleared the textarea; the open composition then re-committed the last char into the empty field, duplicating it. Submit/edit/DM-send Enter handlers are guarded with `!e.nativeEvent.isComposing`

---

## Where it diverged from the original design

[DESIGN.md](DESIGN.md) covers P0–P5 plus PR review. Everything below is what actually got built on top of it.

| Axis | Status |
|---|---|
| Per-user API keys (§15 "extension seam") | **Built** — per-user encrypted tokens with per-author resolution (07-22) |
| Webhook intake (§16 "not implemented") | **Built** — per-repo secret with a polling toggle (v1.7.0) |
| Full git GUI (§15 "out of scope") | **Partly built** — commit, push, pull, branches, remotes, init/publish, diffs, history graph (through v1.9.0) |
| LLM Wiki | Added after the design — the **fourth workspace entity** (07-21) |
| DM and group chat | Added after the design (07-29) |
| Admin config registry | The design's "global settings tuning" (P5) grown into a runtime-editable registry (07-28) |
| Local session import | Added after the design (07-29) |
| Unified search · shortcuts · context menu | Added after the design (v1.4.0) |
| Guide agent | Added after the design — calls the workspace's own API with your permissions (v1.8.0) |
| Self-update | Added after the design (v1.9.0) |
| Non-essential egress blocking | Added after the design — nine channels, individually controllable (v1.2.0–v1.3.1) |
| SSO / proxy-header auth (§15) | Not implemented |
| Postgres · Redis promotion (§15) | Not implemented |
| CRDT live collaborative editing (§15) | Still out of scope |
