# Loaded by `vite build --mode demo` (npm run build:demo). Turns on the static demo
# network mock. Do NOT set this for the normal build/dev — the app then talks to the
# real backend as usual.
VITE_DEMO=1
